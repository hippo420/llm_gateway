apiVersion: 1
datasources:
  - name: Prometheus
    # 대시보드 JSON 이 이 uid 로 datasource 를 참조한다. 바꾸지 말 것.
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    jsonData:
      timeInterval: 15s
